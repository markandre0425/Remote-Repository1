Edit example.txt or create a new file in the cloned repository.2. Add and commit your changes as previously shown (git add and git commit).3. Push the changes: sh git push 4. To demonstrate git pull, have someone else make a change to the repository, or make a change directly on GitHub.5. Run: sh git pull origin master  This command fetches and merges changes from the remote repository to your local repository.
